def calcValue([LABEL] , [CITY]):
  label = [LABEL]
  city = [CITY]
  if bool(label):
    return label
  else:
    return "City"

def calcValue(LABEL):
  label = [LABEL]
  if (label):
    return label
  else:    return "City"
      